 

<%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }, html:{class: 'form-signin'}) do |f| %>

 <h3  class="text-center">Forgot your password?</h3>
 <%= f.error_notification %>

  
    <%= f.input :email, required: true, autofocus: true, label:false, placeholder: "email@email", input_html: {class: 'form-control'} %>
  

  
    <%= f.button :submit, "Send me reset password instructions", class: "btn btn-primary btn-block" %>
  
<% end %>

<%= render "devise/shared/links" %>
